for routing use a service to get the title of the list
then add to title of template page
on the template page load the list of said title by loading
only the ones tagged the same as the title name

restructure firebase so that each poem has a formType

Fix routing issue !!!!!!!!!!!!!!!!!!!
Fix home category buttons
connect Random Poetry with firebase